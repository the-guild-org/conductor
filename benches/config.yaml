server:
  port: 9000

logger:
  format: pretty

sources:
  - id: countries
    type: graphql
    config:
      endpoint: http://localhost:4000

endpoints:
  - path: /graphql
    from: countries

  - path: /test
    from: countries
