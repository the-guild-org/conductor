{
  "name": "conductor-e2-benchmark",
  "version": "1.0.0",
  "packageManager": "pnpm@8.12.1",
  "engines": {
    "node": ">=20",
    "pnpm": ">=8"
  },
  "scripts": {
    "start:server": "node server.mjs",
    "loadtest": "k6 -e GITHUB_PR=$GITHUB_PR -e GITHUB_SHA=$GITHUB_SHA -e GITHUB_TOKEN=$GITHUB_TOKEN run k6.js",
    "start:gw": "cd .. && cargo run --bin conductor -- ./benchmark/gw.yaml",
    "test": "start-server-and-test start:server http://127.0.0.1:4000/_health 'start-server-and-test start:gw http://127.0.0.1:9000/_health loadtest'"
  },
  "dependencies": {
    "graphql": "16.8.1",
    "graphql-yoga": "5.1.0",
    "@faker-js/faker": "8.3.1"
  },
  "devDependencies": {
    "start-server-and-test": "2.0.3"
  }
}
