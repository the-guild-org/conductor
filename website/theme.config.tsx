/* eslint sort-keys: error */
import { defineConfig } from "@theguild/components";

const docsRepositoryBase =
  "https://github.com/the-guild-org/tree/master/website";

export default defineConfig({
  docsRepositoryBase,
  siteName: "CONDUCTOR",
});
